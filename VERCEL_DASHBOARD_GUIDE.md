# Vercel 대시보드 사용 가이드

## 📍 Vercel 대시보드 접속

1. **브라우저에서 접속**
   - https://vercel.com/dashboard
   - GitHub 계정으로 로그인

2. **프로젝트 선택**
   - 대시보드에서 `q-a-installer-support` 프로젝트 클릭
   - 또는 프로젝트 이름 검색

---

## 🔧 Root Directory 설정 (가장 중요!)

### 위치 찾기

1. **프로젝트 페이지 상단 메뉴**
   ```
   [Overview] [Deployments] [Analytics] [Settings] [Insights]
   ```
   → **Settings** 클릭

2. **Settings 페이지 왼쪽 메뉴**
   ```
   General
   Domains
   Environment Variables
   Build & Development Settings
   Git
   ...
   ```
   → **General** 클릭 (기본적으로 선택됨)

3. **General 페이지에서 찾기**
   - 페이지를 아래로 스크롤
   - **"Root Directory"** 섹션 찾기
   - 현재 값이 비어있거나 다른 값일 수 있음

4. **수정하기**
   - Root Directory 옆 **"Edit"** 버튼 클릭
   - 입력 필드에 `web` 입력
   - **"Save"** 버튼 클릭

---

## 🔄 재배포 (Redeploy)

### 방법 1: Deployments 탭에서

1. **Deployments 탭 찾기**
   - 프로젝트 페이지 상단 메뉴에서 **"Deployments"** 클릭
   - 또는 URL: `https://vercel.com/[your-username]/q-a-installer-support/deployments`

2. **배포 목록 확인**
   - 최신 배포가 맨 위에 표시됨
   - 각 배포에는 상태 표시 (✅ Ready, ⏳ Building, ❌ Error)

3. **Redeploy 버튼 찾기**
   - 최신 배포 항목의 **오른쪽 끝**에 **"..."** (세 점 메뉴) 클릭
   - 또는 배포 항목에 마우스 오버 시 나타나는 메뉴
   - 드롭다운 메뉴에서 **"Redeploy"** 선택

### 방법 2: Settings에서 재배포

1. **Settings → General**
2. Root Directory 수정 후
3. 자동으로 재배포가 시작될 수 있음
4. 또는 상단 알림에서 "Redeploy" 클릭

---

## 📊 배포 상태 확인

### Deployments 탭에서

1. **배포 목록**
   - 각 배포의 상태 확인
   - ✅ Ready: 성공
   - ⏳ Building: 빌드 중
   - ❌ Error: 실패

2. **배포 상세 보기**
   - 배포 항목 클릭
   - 빌드 로그 확인
   - 에러 메시지 확인

### 빌드 로그 확인

1. 배포 항목 클릭
2. **"View Build Logs"** 또는 **"Build Logs"** 탭 클릭
3. 빌드 과정 확인:
   - `✓ Compiled successfully`
   - `✓ Generating static pages`
   - 에러 메시지 확인

---

## 🖼️ 화면 구성 (참고)

```
┌─────────────────────────────────────────┐
│  Vercel Dashboard                       │
├─────────────────────────────────────────┤
│  [Overview] [Deployments] [Settings]    │  ← 상단 메뉴
├─────────────────────────────────────────┤
│                                         │
│  프로젝트: q-a-installer-support        │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  Settings                       │   │
│  │                                 │   │
│  │  General                        │   │  ← 왼쪽 메뉴
│  │  Domains                        │   │
│  │  Environment Variables          │   │
│  │  ...                            │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  Root Directory                 │   │
│  │  [Edit]                         │   │  ← 여기!
│  │  현재: (비어있음)                │   │
│  │  변경: web                       │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

---

## 🚨 문제 해결

### "Deployments" 탭이 안 보이는 경우
- 프로젝트가 제대로 연결되지 않았을 수 있음
- 프로젝트를 다시 Import

### "Redeploy" 버튼이 안 보이는 경우
- 배포 항목에 마우스 오버
- 또는 배포 항목 클릭 → 상세 페이지에서 "Redeploy" 버튼 찾기

### Root Directory 설정이 안 보이는 경우
- Settings → General 페이지로 이동
- 페이지를 아래로 스크롤
- "Root Directory" 섹션 찾기

---

## 📝 빠른 체크리스트

- [ ] Vercel 대시보드 접속
- [ ] 프로젝트 선택 (`q-a-installer-support`)
- [ ] Settings → General 이동
- [ ] Root Directory를 `web`으로 설정
- [ ] Save 클릭
- [ ] Deployments 탭으로 이동
- [ ] 최신 배포의 "..." → "Redeploy" 클릭
- [ ] 배포 완료 대기 (2-3분)
- [ ] 배포 URL 접속하여 확인

---

## 🔗 직접 링크

프로젝트가 배포되어 있다면:
- Settings: `https://vercel.com/[username]/q-a-installer-support/settings`
- Deployments: `https://vercel.com/[username]/q-a-installer-support/deployments`

